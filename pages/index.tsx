import Envelope from "../components/Envelope";

export default function Home() {
  return (
    <div style={{
      display: "grid",
      placeItems: "center",
      height: "100%",
    }}
    >
      <Envelope />
    </div>

  );
}
