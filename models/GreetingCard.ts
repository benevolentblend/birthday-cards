import GiftCard from "./GiftCard";

type GreetingCard = {
    color: string,
    giftCard?: GiftCard
    coverText?: string,
    coverImage?: string,
    insideText?: string,
    insideImage?: string,
}

export default GreetingCard;
